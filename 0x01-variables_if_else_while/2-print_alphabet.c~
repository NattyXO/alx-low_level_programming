#include <stdio.h>
#include <string.h>
/**
 * main - prints the alphabet.
 *
 * Return : Always 0 (Success)
 */

int main(void)
{
  
for(int x = 'A'; x <= 'Z'; x++)
{
  x = tolower(x);
    putchar(tolower(x));
}
 putchar('\n');
 return (0);

}
